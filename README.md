# furkanakay

This repository contains simple Python scripts. The `snake.py` file implements a basic terminal-based Snake game using the `curses` module.

## Running the game

```bash
python snake.py
```

Use the arrow keys to control the snake. The game ends if the snake hits the wall or itself.
